<%
  const baseUrl = `${ORIGIN_URL}/${lang}`;
%>
<nav class="menus">
<% for (const menu of data.menus) { %>
  <a href="<%- baseUrl + menu.url %>" class="menu-item"><%= _t(menu.title) %></a>
<% } %>
</nav>