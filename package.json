{
  "name": "tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js --production",
    "dev": "node build.js --development",
    "test": "jest",
    "coverage": "jest --coverage",
    "git-push": "node git-push.js",
    "deploy": "npm run test && npm run build && npm run git-push",
    "prepare": "husky"
  },
  "devDependencies": {
    "chalk": "^4.1.2",
    "ejs": "^3.1.10",
    "esbuild": "0.25.12",
    "fs-extra": "^11.3.2",
    "gh-pages": "^6.3.0",
    "glob": "^11.0.3",
    "html-minifier": "^4.0.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "sprintf-js": "^1.1.3",
    "stylus": "^0.64.0"
  }
}
